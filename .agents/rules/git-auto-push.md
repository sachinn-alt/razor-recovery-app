# Git Workflow Rule

After making changes or completing requested code modifications, prompt/confirm with the user and push the changes directly to GitHub (`origin/main` on `sachinn-alt/razor-recovery-app`).
